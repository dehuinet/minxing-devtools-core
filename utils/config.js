const root = require('user-home');

const TEMP_NAMESPACE = 'minxing-devtools';
const TEAMPLATE_TEMP = `${root}/${TEMP_NAMESPACE}/templates`;
const LOCALSTORAGE_TEMP = `${root}/${TEMP_NAMESPACE}/localstorage`;

module.exports = {
    TEAMPLATE_TEMP,
    LOCALSTORAGE_TEMP
};
